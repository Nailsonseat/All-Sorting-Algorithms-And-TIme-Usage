# All Sorting Algorithms And Time Usage
The project uses chrono library to measure the function time.
The size of the array used is 250 , through it can be changed anytime.
And the range of the values wihtin the array used is 0 to 1000.
